import 'package:flutter/material.dart';

class qqImage extends StatelessWidget {
  const qqImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        width: 200,
        child: Image.asset('name'),
      ),
    );
  }
}
